{
  "name": "ideabro",
  "private": true,
  "version": "0.1.0",
  "description": "Ideabro — AI UGC, ad and campaign video studio powered by Gemini Omni 1.1 Flash (via APIMart) and FFmpeg",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c magenta,cyan \"npm:dev -w server\" \"npm:dev -w web\"",
    "build": "npm run build -w web && npm run build -w server",
    "start": "npm run start -w server",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w web",
    "test": "npm run test -w server"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
